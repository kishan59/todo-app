import asyncHandler from "express-async-handler";

const getTaskList = asyncHandler(async (req, res) => {
    console.log("todo routes", req);
    try {
        
    } catch (err) {
        
    }
});

export { getTaskList };